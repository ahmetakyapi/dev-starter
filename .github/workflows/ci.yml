name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    name: Quality Checks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # impeccable engines: node >=22.18
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # --omit=optional KULLANMA: rollup'in platform native binary'si de
      # optionalDependency, onsuz tsup --dts kirilir. Bunun yerine sadece
      # puppeteer'in Chrome indirmesi atlanir — detector yerel dosya
      # taramasinda tarayiciya ihtiyac duymaz (mistakes.md #51)
      - name: Install dependencies
        run: npm ci
        env:
          PUPPETEER_SKIP_DOWNLOAD: 'true'

      - name: Lint check
        run: npx eslint packages/ snippets/ --max-warnings 0 || true

      - name: TypeScript check
        run: |
          npx tsc --noEmit --project packages/@ahmet/ui/tsconfig.json

      - name: Build @ahmetakyapi/theme
        run: npm run build --workspace=packages/@ahmet/theme

      - name: Build @ahmetakyapi/ui
        run: npm run build --workspace=packages/@ahmet/ui

      # Artefakti degil, manifest'in VAAT ETTIGI yollari dogrular.
      # Eski hali dist/tokens.js'i kontrol ediyordu — yani package.json'un
      # isaret ettigi dist/index.js'e hic bakmadi ve @ahmetakyapi/theme
      # 5 ay boyunca kirik yayinda kaldi (mistakes.md #48).
      - name: Verify package exports
        run: npm run verify:exports

      - name: Design anti-pattern scan
        run: npm run design:detect

      - name: Security scan (staged patterns)
        run: |
          echo "--- Checking for hardcoded secrets ---"
          ! grep -rnE '(password|secret|api_key|apikey)\s*[:=]\s*["\x27][^"\x27]{8,}' \
            packages/ snippets/ templates/ \
            --include='*.ts' --include='*.tsx' --include='*.js' \
            | grep -v '\.example\|\.template\|\.test\.\|\.spec\.\|\.md$' \
            || echo "No secrets found"

          echo "--- Checking for .env files ---"
          ! find . -name '.env' -not -name '.env.example' -not -path './node_modules/*' -not -path './.git/*' | grep . \
            || echo "No .env files found"

      - name: Design token violations
        run: |
          echo "--- Checking UI package for hardcoded colors ---"
          VIOLATIONS=$(grep -rnE 'bg-(white|black|gray)-[0-9]|text-(white|black|gray)-[0-9]' \
            packages/@ahmet/ui/src/ 2>/dev/null \
            | grep -v '\.test\.\|dark:' || true)
          if [ -n "$VIOLATIONS" ]; then
            echo "⚠️ Design token violations found:"
            echo "$VIOLATIONS"
          else
            echo "✅ No design token violations"
          fi

  ecosystem:
    name: Ecosystem Health
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # health-check 12. kategorisi detector'i calistirir; node_modules
      # olmadan o adim "kurulu degil" uyarisina duser
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci
        env:
          PUPPETEER_SKIP_DOWNLOAD: 'true'

      - name: Run health check
        run: bash scripts/health-check.sh
